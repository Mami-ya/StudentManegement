<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="student.manatement.StudentManegement.repository.StudentRepository">

  <!-- 受講生の全件検索 -->
  <select id="search" resultType="student.manatement.StudentManegement.data.Student">
    SELECT
    student_id,
    name,
    name_kana,
    nickname,
    email,
    area,
    age,
    gender,
    remark,
    is_deleted AS deleted
    FROM student
  </select>

  <!-- 受講生の検索 -->
  <select id="searchStudentId" resultType="student.manatement.StudentManegement.data.Student">
    SELECT
    student_id,
    name,
    name_kana,
    nickname,
    email,
    area,
    age,
    gender,
    remark,
    is_deleted AS deleted
    FROM student
    WHERE student_id = #{studentId}
  </select>

</mapper>